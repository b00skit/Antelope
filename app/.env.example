# This file is an example of the environment variables needed for the application.
# Copy this file to .env and fill in the values for your local development.

# Generate a secure, random string (at least 32 characters long) for the cookie password.
# You can use `openssl rand -hex 32` to generate one.
SECRET_COOKIE_PASSWORD=""

# A secret token used once to grant the first user superadmin privileges.
# This should be a long, random, and unguessable string.
SETUP_TOKEN=""

# GTA:W OAuth Configuration (Optional)
# To enable, set GTAW_AUTH_ENABLED to "true" and fill out the other values.
GTAW_AUTH_ENABLED="false"
GTAW_CLIENT_ID=""
GTAW_CLIENT_SECRET=""
GTAW_CALLBACK_URL="http://localhost:3004/api/auth/callback" # Adjust if your local port is different

# Database configuration (defaults to SQLite if not set)
# DATABASE=sqlite
# DB_FILE_NAME=sqlite/local.db

# For MySQL/MariaDB, use these instead:
# DATABASE=mysql
# DB_IP=127.0.0.1
# DB_PORT=3306
# DB_NAME=antelope
# DB_USERNAME=root
# DB_PASSWORD=password
